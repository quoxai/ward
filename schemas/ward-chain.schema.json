{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/quoxai/ward/schemas/ward-chain.schema.json",
  "title": "WARD Chain Descriptor",
  "description": "A ward chain descriptor (v0.1). Summarizes chain metadata for management purposes. See SPEC.md section 7.",
  "type": "object",
  "required": [
    "chain_id",
    "genesis_hash",
    "scope",
    "created_at",
    "entry_count",
    "head_seq",
    "head_chain_hash"
  ],
  "properties": {
    "chain_id": {
      "type": "string",
      "description": "Unique chain identifier, e.g. \"ward:org/quox/env/production\"",
      "minLength": 1
    },
    "genesis_hash": {
      "type": "string",
      "description": "SHA-256 hex of \"WARD-GENESIS|\" + chain_id",
      "pattern": "^[0-9a-f]{64}$"
    },
    "scope": {
      "type": "string",
      "description": "Human-readable scope description",
      "minLength": 1
    },
    "created_at": {
      "type": "string",
      "description": "ISO 8601 timestamp with UTC Z suffix — when the chain was created",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$"
    },
    "entry_count": {
      "type": "integer",
      "description": "Total number of entries in the chain",
      "minimum": 0
    },
    "head_seq": {
      "type": "integer",
      "description": "Sequence number of the latest entry (0 if chain is empty)",
      "minimum": 0
    },
    "head_chain_hash": {
      "type": "string",
      "description": "chain_hash of the latest entry, or genesis_hash if chain is empty",
      "pattern": "^[0-9a-f]{64}$"
    }
  },
  "additionalProperties": true
}
