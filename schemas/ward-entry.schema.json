{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/quoxai/ward/schemas/ward-entry.schema.json",
  "title": "WARD Entry",
  "description": "A single ward entry (v0.1). A content-free hash-chain receipt witnessing a source event. See SPEC.md section 6.",
  "type": "object",
  "required": [
    "ward_version",
    "ward_entry_id",
    "chain_id",
    "seq",
    "witnessed_at",
    "source_kind",
    "source_id",
    "payload_hash",
    "prev_chain_hash",
    "chain_hash",
    "issuer_id"
  ],
  "properties": {
    "ward_version": {
      "type": "string",
      "description": "Protocol version, e.g. \"0.1\"",
      "pattern": "^[0-9]+\\.[0-9]+$"
    },
    "ward_entry_id": {
      "type": "string",
      "description": "Unique identifier for this entry within the chain",
      "minLength": 1
    },
    "chain_id": {
      "type": "string",
      "description": "Identifier for the chain this entry belongs to",
      "minLength": 1
    },
    "seq": {
      "type": "integer",
      "description": "Monotonically increasing sequence number, starting at 1",
      "minimum": 1
    },
    "witnessed_at": {
      "type": "string",
      "description": "ISO 8601 timestamp with UTC Z suffix — when WARD observed the source event",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$"
    },
    "source_kind": {
      "type": "string",
      "description": "The kind of source event being witnessed",
      "enum": ["AEE", "AOCL", "VOLT", "WARD", "EXTERNAL"]
    },
    "source_id": {
      "type": "string",
      "description": "Identifier of the source event. For WARD meta-chain entries, format is <chain_id>::<tip_seq>",
      "minLength": 1
    },
    "payload_hash": {
      "type": "string",
      "description": "SHA-256 hex digest of the source event's canonical content",
      "pattern": "^[0-9a-f]{64}$"
    },
    "prev_chain_hash": {
      "type": "string",
      "description": "Previous entry's chain_hash, or genesis_hash for seq=1",
      "pattern": "^[0-9a-f]{64}$"
    },
    "chain_hash": {
      "type": "string",
      "description": "This entry's computed chain hash (see SPEC.md section 4.4)",
      "pattern": "^[0-9a-f]{64}$"
    },
    "issuer_id": {
      "type": "string",
      "description": "Identifier of the WARD instance that created this entry",
      "minLength": 1
    },
    "source_ts": {
      "type": "string",
      "description": "ISO 8601 timestamp from the source event itself (optional)",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$"
    },
    "tags": {
      "type": "array",
      "description": "String tags for filtering and categorization",
      "items": {
        "type": "string"
      }
    },
    "sig": {
      "type": "string",
      "description": "Ed25519 signature over chain_hash (base64-encoded, optional)",
      "minLength": 1
    }
  },
  "additionalProperties": true
}
