{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/quoxai/ward/schemas/ward-tip.schema.json",
  "title": "WARD Tip",
  "description": "A ward tip/checkpoint (v0.1). Summarizes chain state at a point in time with optional Ed25519 signing. See SPEC.md section 8.",
  "type": "object",
  "required": [
    "tip_id",
    "chain_id",
    "tip_seq",
    "tip_chain_hash",
    "entry_count",
    "created_at"
  ],
  "properties": {
    "tip_id": {
      "type": "string",
      "description": "Unique identifier for this tip",
      "minLength": 1
    },
    "chain_id": {
      "type": "string",
      "description": "The chain being checkpointed",
      "minLength": 1
    },
    "tip_seq": {
      "type": "integer",
      "description": "The sequence number of the entry being checkpointed",
      "minimum": 1
    },
    "tip_chain_hash": {
      "type": "string",
      "description": "chain_hash of the entry at tip_seq",
      "pattern": "^[0-9a-f]{64}$"
    },
    "entry_count": {
      "type": "integer",
      "description": "Number of entries in the chain up to and including tip_seq",
      "minimum": 1
    },
    "created_at": {
      "type": "string",
      "description": "ISO 8601 timestamp with UTC Z suffix — when the tip was created",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$"
    },
    "sig": {
      "type": "string",
      "description": "Ed25519 signature over tip_chain_hash hex bytes (base64-encoded, optional but SHOULD be present)",
      "minLength": 1
    },
    "key_id": {
      "type": "string",
      "description": "Stable identifier for the signing key (DID or fingerprint)",
      "minLength": 1
    },
    "sink_ref": {
      "type": "string",
      "description": "Reference to external sink where this tip was published (e.g., Gitea tag URL, S3 object key)",
      "minLength": 1
    },
    "notes": {
      "type": "string",
      "description": "Human-readable annotation for this tip"
    }
  },
  "additionalProperties": true
}
